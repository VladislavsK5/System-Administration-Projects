PowerShell script for periodic file or directory archiving

## Execution
Run via PowerShell(and with administrator rights):
.\backupscript.ps1


If script execution is disabled on your system, run the following command once to allow local scripts:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
